{
  "name": "relentless",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "style": "app/styles/**/*.css",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "NODE_PATH=app watchify -t babelify app/app.js -o app.js -p [ parcelify -wo app.css -v ] -v"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babelify": "^6.3.0",
    "parcelify": "^2.1.0",
    "react-dom": "^0.14.0",
    "routes": "^2.1.0"
  }
}
